#include <stdio.h>
#include <stdlib.h>
/**
 * _abs - computes the absolute value of an integer.
 *
 * @int: holds a inteher value.
 *
 * Return: 0 on success.
 *
 */
int _abs(int);

int _abs(int num)
{
	return (abs(num));
}
